# 📐 قواعد الكود — إجباري على كل من يعدّل هذا المشروع

> ## ⚠️ القاعدة الذهبية
> **١) لا يزيد أي ملف كود عن ١٠٠٠ سطر — بدون أي استثناء.**
> **٢) الكود في أماكنه الثابتة فقط (الترتيب المعماري أدناه) — ممنوع ملفات أو أكواد عشوائية.**
>
> ملف قارب على الـ١٠٠٠ سطر؟ قسّمه فورًا إلى ملفات أصغر حسب الوظيفة، بأسماء واضحة تصف دور كل ملف.

---

## 🏛️ الترتيب المعماري (خريطة المشروع)

```
Logistics/
├── app.py                 ← تشغيل + ربط المسارات فقط (لا منطق هنا)
├── config.py              ← الثوابت: أسماء الأقسام والشهور (مصدر الحقيقة الوحيد)
├── database.py / db_*.py  ← كل تعامل مع قواعد البيانات — كل ملف مسؤوليته واحدة
├── storage.py             ← إدارة مجلدات database/ (السنوات/الشهور/الأقسام)
├── arabic_numbers.py      ← محرك الأرقام العربية (الإدخال/العرض) + التحويلات لاحقًا
├── routes/                ← ملف مسارات مستقل لكل مجموعة صفحات (عند التوسع)
├── templates/
│   ├── base.html          ← الهيكل العام + partials صغيرة
│   └── <قسم>.html         ← قالب مستقل لكل صفحة
├── static/css/            ← CSS مقسّم بالغرض (theme / layout / components / pages)
├── static/js/             ← JS مقسّم بالغرض
├── database/              ← البيانات فقط (محلي 100% — لا يُرفع على GitHub أبدًا)
│   ├── system.db
│   ├── الدباجة/           ← اللوجو + ملف Word الدباجة
│   └── <السنة>/<الشهر>/<القسم>/  ← ملفات Excel لكل قسم
├── CONTRIBUTING.md        ← هذا الملف
└── README.md              ← التعريف والتشغيل
```

| القاعدة | التفصيل |
|---|---|
| **اسم الملف يصف محتواه** | ممنوع `utils.py` / `misc.py` عشوائية |
| **فصل الطبقات** | عرض (templates) ≠ منطق (routes) ≠ بيانات (db_* / storage) |
| **ملف واحد = وظيفة واحدة** | أي ملف يتجاوز دوره يُقسَّم فورًا |

## 🗄️ قواعد البيانات

- مجلد `database/` **محلي 100%** — لا يُرفع على GitHub ولا يُحذف من `.gitignore`.
- كل سنة مجلد مستقل، و**كل شهر منعزل تمامًا** — ممنوع خلط بيانات شهر بآخر.
- أسماء الأقسام والشهور تُجلب من `config.py` فقط — ممنوع كتابتها يدويًا في أي ملف آخر.
- إضافة سنوات/حذفها يتم من شريط الأدوات العلوي داخل البرنامج فقط.
- ملفات Excel لكل قسم **مُحدّثة لحظيًا** — أي تعديل في جدول القسم يعيد بنائها تلقائيًا.

## 🔢 قواعد الأرقام والإدخال

- الإدخال **بالعربية والأرقام العربية المشرقية (٠١٢٣...)** أساسًا، مع قبول الإنجليزية.
- أي قراءة/عرض لأرقام يمر عبر `arabic_numbers.py` فقط — ممنوع تحويلات يدوية مبعثرة.
- التخزين داخليًا بأرقام غربية، والعرض للمستخدم بأرقام عربية مشرقية.
- **قاعدة الكميات**: تُعرض دائمًا **بثلاثة أرقام عشرية** عبر `fmt_qty` — `0.12 → ٠٫١٢٠` وحتى الصفار في الخانة الثالثة يُكتب `75 → ٧٥٫٠٠٠`.

## 📜 قواعد الدباجة والملفات الرسمية

- الدباجة (أسطرها الأربعة + اللوجو) والتوقيعان (رتبة/اسم — يمين وشمال أسفل المستند) **مركزية**:
  تُدار فقط من صفحة «الدباجة والتوقيعات الرسمية» وتُخزن في `database/الدباجة/` + جدول `app_settings`.
- **كل** ملفات Excel/Word الرسمية في كل المجلدات تلتزم بها — أي مولّد ملفات جديد يجب أن يقرأها من هذه المصادر نفسها.

## 📄 قواعد الصفحات

- **لكل قسم صفحته المستقلة** بملفاتها الخاصة — ممنوع تكديس قسمين في ملف واحد.
- أي تعديل تصميم/وظيفة لقسم يُنفَّذ في ملفات ذلك القسم فقط.

## ✅ حالة الالتزام الحالية

كل ملفات المشروع حاليًا ملتزمة بالحد (أقل من ١٠٠٠ سطر). حافظ على ذلك:
أي ملف يقترب من ٧٠٠–٨٠٠ سطر يبدأ التخطيط لتقسيمه قبل الوصول للحد.
