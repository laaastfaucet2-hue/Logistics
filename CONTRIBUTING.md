# قواعد المشروع — ملزمة

## القواعد الذهبية
١. لا يزيد أي ملف كود عن ١٠٠٠ سطر. عند ٧٠٠–٨٠٠ سطر خطّط لتقسيمه حسب الوظيفة.
٢. كل ملف في مجلد وظيفته فقط. ممنوع إضافة ملفات عشوائية بجذر المشروع.
٣. اعرض خطة قبل التغييرات الجوهرية واحصل على موافقة المستخدم، واختبر كل دفعة تغييرات.
٤. الاختبارات والبيانات التجريبية في مجلد مؤقت معزول فقط؛ لا تضع بيانات تجريبية في شهور المستخدم.

## خريطة الملفات المعتمدة

```
Logistics/
├── app.py                 تشغيل وربط فقط (application factory)
├── START.bat              تشغيل نسخة المصدر على Windows
├── core/                  config.py، arabic_numbers.py، egtime، الصلاحيات والمسارات
├── data_access/           database.py، months.py، db_*، storage، dataguard
├── services/              العمليات: النسخ، الترحيل، الإشعارات، تجهيز البيانات
├── documents/             توليد Excel وWord والدباجة الرسمية
├── routes/                ملف مسارات مستقل لكل مجموعة صفحات
├── templates/             قالب مستقل لكل صفحة، وpartials للقطع المشتركة
├── static/
│   ├── css/               تنسيق مقسم بالغرض
│   ├── js/                سلوك مقسم بالغرض
│   ├── fonts/             خطوط محلية + تراخيصها
│   ├── img/               أصول الواجهة وأيقونة التطبيق
│   └── vendor/            أصول الطرف الثالث المحلية + تراخيصها
├── desktop/               النافذة الأصلية، الجسر المحدود، ونقطة دخول Windows
├── scripts/installer/     وصف بناء التطبيق والمثبّت
├── requirements/          متطلبات سطح المكتب والبناء (الأساسية في requirements.txt)
├── tests/                 اختبارات معزولة عن بيانات المستخدم
├── docs/                  التشغيل وخطة التطوير
└── database/              بيانات محلية فقط؛ ممنوع رفعها لـGitHub
```

الجذر يبقى للتشغيل والمتطلبات وREADME وتعليمات المساهمين فقط.
أسماء الملفات تصف وظيفتها؛ ممنوع `utils.py` أو `misc.py` تجمع مسؤوليات غير مرتبطة.
فصل العرض عن منطق العمليات عن طبقة البيانات إلزامي.

## أسماء الأقسام والأرقام

- `core/config.py` مصدر أسماء الأقسام والشهور الوحيد.
- كل تحويل/عرض أرقام يمر عبر `core/arabic_numbers.py`.
- التخزين بأرقام غربية والعرض بعربية مشرقية. الكميات دائمًا عبر `fmt_qty` بثلاث خانات: `٠٫١٢٠`.
- لا تظهر `None`: حقول الأرقام `٠`، الجداول `—`، خلايا Excel الفارغة بلا نص.

## عزل البيانات الشهري

```
database/
├── system.db                 الحسابات والجلسات وإعدادات تشغيل عامة فقط
├── backups/                  آخر ٤٠ نسخة محلية
└── <السنة>/<رقم-الشهر>/
    ├── month.db              المقررات والجهات وأصل القوة واليومية وإعدادات الدباجة
    ├── الدباجة/              اللوجو وملف الدباجة لهذا الشهر
    ├── 08-المجندين/
    │   ├── ملفات/            الصور والشهادات
    │   └── مستندات/          الكشوف والتصاريح
    └── <مجلدات بقية الأقسام>/
```

- كل دالة بيانات شهرية تستقبل السنة والشهر صراحة. لا تعتمد طبقة البيانات على Flask أو شهر عالمي.
- تعديل/حذف في شهر لا يغيّر غيره. النسخ لقطة مستقلة، ولا ينسخ اليومية مع أصل القوة.
- نسخ المجندين لا يستبدل سجلًا له نفس الرقم العسكري. استبدال الدباجة يحتاج تأكيدًا.
- البيانات القديمة لا تُوزع تلقائيًا على الشهور: استيراد صريح ونسخة احتياطية، مع الحفاظ على روابط اليومية.
- ملفات `recruits.db` و`الدباجة/` القديمة إن وُجدت تُترك كمصدر ترحيل، ولا تُحذف تلقائيًا.
- إضافة وحذف السنوات من البرنامج. جميع مسارات البيانات من `core/paths.py` و`storage.py`، لا من CWD.
- المثبّت يفصل البيانات عن ملفات البرنامج. لا تُضمّن أي قاعدة بيانات أو ملفات مستخدم في حزمة البناء.

## الدباجة الرسمية

الدباجة مركزية **داخل كل شهر فقط**، عبر `data_access/db_letterhead.py` ومجلد الشهر.
كل مولّد Excel/Word يقرأ نسخة الشهر نفسه: الأسطر الأربعة، اللوجو، وتوقيعا اليمين والشمال.
اللوجو يجب أن يكون صورة مضمنة داخل المستند الفعلي، لا رابطًا ولا معاينة HTML فقط.
تعديل الدباجة يحدّث مستندات الشهر وحده. ملفات Excel القديمة تُحدّث عند تنزيلها.

## الواجهة

- الخط الوحيد للواجهة هو IBM Plex Sans Arabic، مضمن في `static/fonts/` مع ترخيص OFL؛ الأوزان الحقيقية ٤٠٠/٥٠٠/٦٠٠/٧٠٠.
- `static/css/theme.css` مصدر ألوان الواجهة: أحمر `#DA291C`، أصفر `#FFC72C`، وكتابة سوداء على الأصفر. الخلفيات الفاتحة للجداول؛ ألوان الحالات منفصلة ولا تعتمد على اللون وحده.
- `desktop/appearance.py` يجهز شاشة البداية بنفس ملفات الخط والثيم، مضمّنة محليًا قبل تشغيل الخادم.
- لا تُطبق هوية الواجهة على ألوان المستندات الرسمية أو لوجو المستخدم.
- جميع القوائم بمكوّن `input[data-combo]` من `static/js/combo.js`؛ لا قوائم datalist بيضاء خام.
- أزرار إجراءات الصفوف SVG بكلاس `icon-btn` وtitle (edit/del/print/save/sync/down/zip)، لا إيموجي.
- طباعة رسمية فقط بالدباجة والتوقيعات.
- موارد التشغيل محلية بالكامل (خطوط وJS وCSS). لا اعتماد على CDN أو الإنترنت في النسخة المثبتة.
- نسخة سطح المكتب لا تفتح متصفحًا للصفحات الداخلية. الجسر الأصلي محدود بأوامر موثوقة.

## حماية البيانات

- كل كتابة ملف بيانات عبر `dataguard.atomic_save`؛ ممنوع الحفظ المباشر.
- SQLite عبر `months.get_db` / `database.get_conn`، مع WAL وbusy_timeout وsynchronous.
- النسخ الاحتياطي يأخذ SQLite snapshots متسقة حتى عند وجود WAL، ويكتب ZIP ذرّيًا.
- التدوير يحتفظ بآخر ٤٠ نسخة، ولا يرفع `database/` مطلقًا.
- startup_guard يفحص السلامة عند بدء التشغيل. لا تغيّر البيانات أو تنفذ ترحيلًا عند import ملفات الكود.
- قبل إعلان اكتمال نسخة Windows: بناء فعلي، اختبار EXE المثبت، واختبار واجهة أصلي على Windows.
