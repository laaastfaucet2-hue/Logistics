# 🤖 تعليمات إلزامية لأي مساعد آلي (AI Agent) يعمل على هذا المشروع

## قف — قبل أي تعديل أو ملف جديد

هذا الصندوق أقصر عمدًا. تجاهله = الشغل مرفوض. Arena/Cursor يقرآن أول الملف؛ التفاصيل المرقّمة تحته بنفس القوة.

1. اقرأ بالترتيب **كاملًا**: هذا الملف ← `CONTRIBUTING.md` ← `core/config.py` (الأقسام، الشهور، الوحدات، `STATIC_VER`). ممنوع كود قبل الثوابت.
2. لا ملف فوق **١٠٠٠ سطر**. لا ملفات عشوائية في جذر المشروع.
3. **خطة + موافقة المستخدم** قبل تغيير جوهري أو إعادة تنظيم ملفات.
4. `database/` محلي لكل شهر — لا GitHub، ولا بيانات تجريبية في شهور المستخدم.
5. الأرقام والكميات من `core/arabic_numbers.py` فقط؛ الكمية ثلاث خانات عربية (`fmt_qty`).
6. القوائم: `input[data-combo]` فقط. ممنوع `<select>` أو datalist أبيض ظاهر. الصف الديناميكي يستدعي `LogisticsCombo.enhance`.
7. هوية الواجهة: **Tajawal** + خلفية slate `#020617` + تمييز amber `#F59E0B`. البنفسجي وخط Changa **ليسا** هوية الواجهة. الورق الرسمي لا يُمس.
8. كل صفحة/تبويب: زر طباعة (`partials/print_btn.html` / `js-print-page`) + دباجة أعلى + توقيعان أسفل. flash/toast تُغلق بـ ✕. ممنوع `None` على الواجهة.
9. البرنامج محلي بالكامل (بلا CDN/إنترنت). بعد التعديل: اختبر ثم أعلن الإنجاز.
10. إن تعارض سطر قديم في هذا الملف أو في `CONTRIBUTING.md` مع هذا الصندوق أو مع `core/config.py` — **الصندوق والثوابت يغلبان**، وحدّث السطر القديم في نفس الدفعة.

---

اقرأ `CONTRIBUTING.md` كاملًا قبل أي تعديل. الخلاصة غير القابلة للتفاوض:

1. **لا يزيد أي ملف عن ١٠٠٠ سطر.** عند الاقتراب من الحد: قسّمه فورًا بأسماء واضحة.
2. **الترتيب المعماري ثابت** (انظر خريطة `CONTRIBUTING.md`): ممنوع ملفات أو أماكن عشوائية.
3. أسماء الأقسام/الشهور من `core/config.py` فقط. الأرقام عبر `core/arabic_numbers.py` فقط.
4. `database/` مجلد بيانات محلي — لا يُرفع على GitHub أبدًا، وكل شهر فيه منعزل تمامًا.
5. اعرض خطة على المستخدم واحصل على موافقته **قبل** تنفيذ أي تغيير جوهري.
6. اختبر عملك بعد كل تعديل (تشغيل + طلبات فعلية) قبل إعلان الإنجاز.
7. **كل الكميات تُعرض بثلاثة أرقام عشرية عربية دائمًا** (٠٫١٢٠ و٧٥٫٠٠٠) عبر `arabic_numbers.fmt_qty` فقط.
8. **الدباجة والتوقيعات مركزية داخل كل شهر فقط**: تُدار من صفحة «الدباجة والتوقيعات الرسمية» وتلتزم بها ملفات Excel/Word للشهر نفسه فقط. اللوجو والأسطر يجب أن تكون داخل المستند الحقيقي.
9. **قواعد توزيع الجهات**: الجهة الجديدة تُسحب لها أصناف المقرر النشط في قسمها نفسه بأرقامها (ممنوع السحب من قسم آخر). تخصيص الأيام يُحفظ دفعة واحدة بزر واحد (بدون ريفرش لكل يوم) ولكل يوم مقرر خاص اختياري. لا تُضاف بيانات تجريبية في قواعد الشهور أبدًا — البيانات كلها من المستخدم.
10. **قاعدة القوائم المنسدلة (مكرّرة بصرامة ٢٣/٠٩/٢٠٢٦ بعد رفض المستخدم ظهور قائمة نوع الملحقة البيضاء)**: شكل أي قائمة منسدلة في أي شاشة بالمنظومة = نفس ثيم قائمة الشهور (كحلي غامق + تحديد آمبر كهرماني) عبر مكوّن `static/js/combo.js` وسمة `input[data-combo]` على الحقل. **ممنوع أي `list=` الخام (datalist أو select أصلي) على الواجهة أبدًا** — البيانات تبقى في `<datalist id>` خفية ويقرؤها الكومبو. وكل صف/حقل يُضاف ديناميكيًا بالجافاسكربت (كصفوف الملحقات) يجب استدعاء `window.LogisticsCombo.enhance(الجذر)` بعد إضافته، ويُختبر ذلك في أطقم المتصفح (الفحص على `data-combo-ready`).
11. **قاعدة القيم الفارغة**: ممنوع ظهور كلمة `None` على أي واجهة. الفارغ يُعرض `٠` داخل حقول الإدخال (فلتر `oval`) و`—` داخل الجداول (ماكرو `num`) وخلية فاضية في الإكسل (`_num`).
12. **قاعدة أزرار الإجراءات**: أي زر تعديل/حذف/طباعة/حفظ/مزامنة في أي صف أو لوحة = أيقونة SVG احترافية بكلاس `icon-btn` — التعديل `edit` (ترس ⚙)، الحذف `del` (سلة قمامة)، الطباعة `print` (طابعة)، الحفظ `save`، إعادة السحب/المزامنة `sync` — مع `title` توضيحي. ممنوع الإيموجي (✏️🗑) أو الأزرار النصية لإجراءات صفوف الجداول. وأي زر طباعة يجب أن يُخرج مستندًا رسميًا بالدباجة والتوقيعات (طباعة احترافي فقط). وللتنزيلات: أيقونة `down` للملفات المباشرة وأيقونة `zip` للمضغوطات؛ وعند الاشتباه يكون تنزيل الإكسل المباشر هو الأساسي. أزرار الشريط تُبنى بأيقونة SVG داخلها لا بالإيموجي.

13. **قاعدة حماية البيانات**: كل كتابة ملف بيانات (إكسل/وورد/لوجو) تمر عبر `dataguard.atomic_save` (ملف مؤقت + فحص ZIP + استبدال ذرّي) — ممنوع `save()` المباشر. كل اتصال SQLite إلزاميًا براجمات الحماية (WAL + busy_timeout + synchronous) عبر `months.get_db`/`database.get_conn` فقط. النسخ الاحتياطية في `database/backups/` محلية ولا تُرفع لجيتهاب مطلقًا، والتدوير يحتفظ بآخر ٤٠ نسخة. `dataguard.startup_guard` يعمل عند تشغيل النظام: يفحص سلامة كل الملفات ويحجز التالف بامتداد ‎.corrupt-الوقت‎ ويسترجع آخر نسخة سليمة تلقائيًا.

14. **تنظيم الملفات**: الخريطة الجديدة في `CONTRIBUTING.md` ملزمة: core / data_access / services / documents / desktop / scripts / tests. الجذر للتشغيل والمتطلبات والتوثيق الأساسي فقط.
15. **العزل والنسخ**: أصل القوة والمرفقات والدباجة داخل الشهر؛ كل دالة بيانات تستقبل year وmonth. النسخ لقطة مستقلة بمعاينة وتأكيد، ولا ينسخ اليومية. استيراد المصادر القديمة صريح ولا يحذفها.
16. **نسخة Windows**: بيانات المثبّت خارج مجلد البرنامج، لا تُضمّن في EXE، ولا تُحذف بالتحديث/إلغاء التثبيت. لا يُعلن أن مثبّتًا جاهزًا أو مجرّبًا دون بناء واختبار فعليين. موارد الواجهة محلية بدون CDN.

17. **الهوية المعتمدة — آخر توجيه للمستخدم (٢٣/٠٩/٢٠٢٦)**: لوحة **slate/amber** مطابقةً لبرنامجه الثاني: الخلفية slate-950 `#020617`، البطاقات slate-900 `#0F172A`، الحدود slate-800 `#1E293B`، والتمييز amber-500 `#F59E0B` (hover ‎`#FBBF24` والنص عليه slate-950). وكتلة التعريف في الشريط الجانبي خمسة أسطر بترتيب مُلزم: وزارة الداخلية ← قطاع الأمن المركزى ← منطقة وسط وجنوب سيناء ← قطاع وسط سيناء ← قسم التعيينات. وخط الواجهة المعتمد: Tajawal** المحلي بأوزان ٤٠٠/٥٠٠/٧٠٠/٨٠٠ (ترخيص OFL في `static/fonts/Tajawal-OFL.txt`) ليكون مطابقًا للبرنامج الثاني؛ Tajawal مُصمم أصليًا بالأرقام العربية المشرقية (٠١٢٣٤٥٦٧٨٩) فيستقر شكل الأرقام في كل موضع بلا خط احتياطي. خط Changa يظل مضمّنًا للتوافق (صفحة الترحيب المضمّنة). القيم من `static/css/theme.css` وأسماء رموزها ثابتة للتوافق (`--brand-gold` تحمل الـamber). الهوية البنفسجية القديمة ملغاة كليًا ولا يجوز إرجاعها. لا ترجع لخطوط Cairo/IBM، ولا تغيّر لوجو الوحدة أو ألوان المستندات الرسمية الورقية.

18. **قاعدة التاميدات — ملزمة**: قسم «التاميدات» (قطاع وسط سيناء - قسم التميينات / منطقة وسط وجنوب للأمن المركزي) شهري العزل بالكامل: القاموس يبدأ فارغًا كل شهر، والجهة غير المعروفة تُضاف عند حفظ التأميدة بعد تنبيه المستخدم فقط. تعدد السجلات لنفس الجهة/اليوم مسموح، واللصق ينشئ سجلًا منفصلًا داخل الشهر نفسه فقط. تنبيه الراغبين (ضباط/أفراد فقط — لا مجندين) تحذير لا يمنع الحفظ. حذف جهة القاموس يحذف تأميداتها داخل هذا الشهر فقط بعد تأكيد قوي. كل حفظ/تعديل/حذف يعلن نجاحه برسالة مرئية صريحة **بعد** اكتمال الكتابة المحلية فعلًا («…وتسجيلها في البيانات المحلية»). الملف المحلي `database/<سنة>/<شهر>/07-التاميدات/` يحوي فولدرات بأسماء التويبات الأربعة تتحدث لقطاتها JSON بعد كل كتابة مع مرآة Excel RTL لكل تبويب، والتقرير الشامل (Excel بالدباجة واللوجو داخل الملف + Word + طباعة/PDF) يُحفظ محليًا في فولدر «طباعة التقرير الشامل». الجهة الملحقة تُعامل جهةً مستقلة في المومدة والقاموس (صف بشارة «ملحقة»، تسجيل تلقائي بالقاموس، عدد تأميدات يشمل مشاركاتها) — «التأميدات غير الجهات»: عرض اليوم تأميدة واحدة بإجمالي مدمج، والإحصائيات بالأعداد × أيام السريان. التأميدة تُدوَّن دائمًا «من يوم/إلى يوم» داخل الشهر النشط فقط (من==إلى = يوم واحد ولا يتحقق زر تفعيل مستقل)، وتظهر سارية في كل أيامها، واللصق يحفظ مدتها بقصّ آخر الشهر. منع الازدواج إلزامي طبقتان: زر الحفظ **يُقفل مؤقتًا أثناء الإرسال** (طلب المستخدم النهائي) ويستعيد نشاطه عند الرجوع للصفحة (pageshow bfcache) + «توكن الحفظ» الخادمي الاحتياطي — الضغط المزدوج/إعادة الإرسال لا ينشئ سجلين أبدًا. أزرار السجل أربعة كما اعتمدها المستخدم: تعديل ذهبي، نسخ برتقالي بالجهات المومدة للصق يوم آخر، زر ظاهر «عمل إذن صرف ٢ مخازن» يفتح الحاسبة على تلك التأميدة وبعد الحفظ شارة «تم عمل إذن ٢ مخازن»، وحذف أحمر بتأكيد صريح. قائمة الإذن اسمها «تحميل قائمة التأميدات»: كل جهة سطر مستقل، الرئيسية وملحقات نفس التأميدة في مربع واحد بلون مميز حسب اليوم، والصرف لا يتجاوز أيام التأميدة نفسها حتى لو نافذة الحاسبة أطول — وطباعة مستند التأميدة رابط أنيق `tm-print-mini` تحت الاسم. جدول اليوم يعرض تأميدة الملحقات **في صف واحد بقيم مكدّسة منسقة** (الأم + كل ملحقة + الإجمالي)، وأيام كل جهة في المومدة تُعرض من زر «افتح الأيام بالتواريخ» (قائمة منبثقة قابلة للطباعة، كل تأميدة من يوم إلى يوم) وتُحفظ في مرآة المومدة المحلية. جدول المومدة **بلا أعمدة متوسطات** (أمر المستخدم، تبويب كما هو) — المتوسطات الثلاثة (ضباط/أفراد/مجندين) تُعرض في تبويب «قاموس ودليل الجهات» مكان أعمدة الراغبين، وبجوارها عدد التأميدات وزر «افتح التواريخ» (من – إلى لكل تأميدة ويُبيّن الملحقة وعلى مَن) وشارة «ملحقة ×عدد مراتها» عند وُجدت، والنافذة المنبثقة واحدة مشتركة في القالب الرئيسي تخدم التبويبين.

19. **قاعدة التواريخ — ملزمة**: كل تاريخ كامل ظاهر للمستخدم أو مُدخل منه يكون **يوم/شهر/سنة `dd/mm/yyyy`** بصفر بادئ وأرقام عربية، مثل **٢٢/٠٩/٢٠٢٦**؛ ممنوع `mm/dd/yyyy` أو عرض ISO في حقول/جداول التاريخ. استخدم `core/dates.py` ومرشحي `datefmt`/`dateinput` ومكوّن `partials/dates.html` مع التقويم العربي المحلي، لا `input[type=date]` الذي يتبع لغة الجهاز. التاريخ معزول اتجاهيًا بـ `bdi dir=ltr`، وكل حسابات اليوم بتوقيت القاهرة. التخزين الداخلي وأسماء الأرشيف التقنية تظل ISO لضمان الفرز والاسترجاع؛ لا تعكس أو ترحّل القيم القديمة عشوائيًا. تُرفض التواريخ المستحيلة قبل حفظ السجل أو مرفقاته. الشهر/السنة وأرقام أيام التقويم ليست تاريخًا كاملاً، ولا يُضاف لها يوم وهمي.

20. **الالتزام بالبرمجة المحلية — ملزم**: البرنامج كله يعمل محليًا على جهاز المستخدم بلا إنترنت ولا خوادم خارجية ولا CDN؛ البيانات كلها في `database/` المحلية فقط (لا تُرفَع لأي سحابة)، وكل مورد (خطوط/أيقونات/مكتبات JS) مضمّن محليًا في `static/`. «الربط المحل» خيار دائم: أي كود يفترض خدمة/شبكة خارجية مرفوض، والمعاينة الويب بديل ناقص عن نسخة سطح المكتب في فتح المجلدات/الملفات فقط — يعلن بذلك برسالة واضحة بدل التظاهر بالنجاح.

21. **قاعدة أزرار الملفات لكل تاب — ملزمة**: لكل تبويب (رئيسي أو فرعي) في المنظومة زرّان بارزان بأسماء صريحة:
    - «📂 فتح مجلد 《اسم التبويب》» يفتح فولدر التبويب المحلي الخاص.
    - «📗 فتح ملف 《اسم الملف.xlsx》» يفتح **ملف Excel** التبويب بذكر اسمه صراحةً على الزر (قرار المستخدم ٢٣/٠٩: كل ملفات أزرار الواجهة Excel؛ Word يبقى للتقرير الرسمي فقط) — ملفات JSON مرايا داخلية لا تظهر على الواجهة.
    داخل فولدرات بيانات الشهر المحلية فقط، وبنفس أسلوب ردود `routes/rations.py` (open_path + رسالة عند التعذر في معاينة الويب). طُبّق أولًا على تبويبات التأميدات الأربعة.
22. **قاعدة رسائل النجاح/الخطأ — ملزمة** (بلاغ المستخدم ٢٣/٠٩/٢٠٢٦ «لا استطيع اغلاق الرسالة»): كل `flash` في المنظومة يحمل زر إغلاق ✕ بكلاس `flash-x` داخل `<span>` للرسالة — إغلاق بضغطة واحدة بالجافاسكربت العام، ولا يجوز إظهار رسالة حفظ مطبوعة بغير زر إغلاق.
23. **قاعدة الطباعة الرسمية — ملزمة** (بلاغ المستخدم ٢٤/٠٩/٢٠٢٦: أي طباعة من البرنامج، وكل التبويبات ٢٤/٠٩): كل صفحة وكل تبويب فرعي يملك زر طباعة (`templates/partials/print_btn.html` بكلاس `js-print-page` — وموجود أيضًا في الشريط العلوي من `base.html`). `window.print()` يخرج مستندًا ورقيًا بالدباجة الرسمية أعلى الصفحة (٤ أسطر الشهر + اللوجو إن وُجد + عنوان عبر `print_doc_title`) والتوقيعين يمين/يسار أسفلها، ويطبع البيانات المهمة في التبويب (الجداول) مع إخفاء الشريط والسايدبار والتوست/الفلاش والنماذج والأزرار. التنفيذ المركزي: `static/css/print.css` + `templates/partials/official_print_*.html` داخل `base.html`. صفحات الطباعة المخصّصة التي تملك دباجتها (`own-letterhead`) تفرّغ `official_print_head`/`official_print_foot` حتى لا تتكرر. ممنوع وضع `.print-doc` بعد الجدول. أي تبويب/صفحة جديدة تُضاف للمنظومة ترث الزر من `base.html` وتملأ `print_doc_title` وتُخفي عناصر التحرير بـ `@media print`.
